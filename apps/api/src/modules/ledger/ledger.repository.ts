import {jakartaDayStart} from './ledger-time.js';import {AsyncLocalStorage} from 'node:async_hooks';import argon2 from 'argon2';import crypto from 'node:crypto';import {Prisma,PrismaClient} from '@prisma/client';import {prisma} from '../../db/client.js';import type {LedgerRepository,LedgerCard,LedgerAccount,LedgerTransaction,LedgerEntry} from './ledger.service.js';
const context=new AsyncLocalStorage<{tx:Prisma.TransactionClient;tenantId:string}>();
const number=(value:unknown)=>Number(value);
/** Approved, parameterized raw SQL is limited to FOR UPDATE and running-balance reads; see docs/LEDGER_SQL_REVIEW.md. */
export class PrismaLedgerRepository implements LedgerRepository {constructor(private readonly client:PrismaClient=prisma){}private tx(){const state=context.getStore();if(!state)throw new Error('Ledger repository method called outside serializable transaction.');return state.tx;}private tenantId(){const state=context.getStore();if(!state)throw new Error('Ledger repository method called outside serializable transaction.');return state.tenantId;}async runSerializable<T>(tenantId:string,work:()=>Promise<T>){return this.client.$transaction(async (tx:Prisma.TransactionClient)=>{await tx.$executeRaw`SELECT set_config('app.current_tenant_id', ${tenantId}, true)`;return context.run({tx,tenantId},work);},{isolationLevel:'Serializable' as any});}async findTransactionByIdempotency(key:string){const row=await this.tx().transaction.findUnique({where:{idempotencyKey:key}});return row?this.transaction(row):undefined;}async findTransactionById(id:string){const row=await this.tx().transaction.findUnique({where:{id}});return row?this.transaction(row):undefined;}async markTransactionVoided(id:string,staffId:string,reason:string){await this.tx().transaction.update({where:{id},data:{status:'VOIDED',voidedBy:staffId,voidedReason:reason}});}async lockCard(id:string,tenantId:string){const rows=await this.tx().$queryRaw<{id:string;tenant_id:string;santri_id:string;status:LedgerCard['status'];pin_hash:string}[]>`SELECT id, tenant_id, santri_id, status, pin_hash FROM cards WHERE id=${id}::uuid AND tenant_id=${tenantId}::uuid FOR UPDATE`;const row=rows[0];return row?{id:row.id,tenantId:row.tenant_id,santriId:row.santri_id,status:row.status,pinHash:row.pin_hash}:undefined;}async verifyPin(value:string,hash:string){return argon2.verify(hash,value);}async lockAccount(tenantId:string,entityType:LedgerAccount['entityType'],entityId:string){const rows=await this.tx().$queryRaw<{id:string;tenant_id:string;entity_type:LedgerAccount['entityType'];entity_id:string}[]>`SELECT id, tenant_id, entity_type, entity_id FROM accounts WHERE tenant_id=${tenantId}::uuid AND entity_type=${entityType} AND entity_id=${entityId}::uuid FOR UPDATE`;const row=rows[0];return row?{id:row.id,tenantId:row.tenant_id,entityType:row.entity_type,entityId:row.entity_id}:undefined;}async runningBalance(accountId:string){const rows=await this.tx().$queryRaw<{balance:unknown}[]>`SELECT COALESCE(SUM(CASE WHEN entry_type='KREDIT' THEN amount ELSE -amount END),0) AS balance FROM ledger_entries WHERE account_id=${accountId}::uuid`;return number(rows[0]?.balance??0);}async successfulSalesToday(tenantId:string,santriId:string,now:Date){const start=jakartaDayStart(now);return number(await this.tx().transaction.aggregate({where:{tenantId,santriId,transactionType:'SALE',status:'SUCCESS',createdAt:{gte:start}},_sum:{amount:true}}).then((result:any)=>result._sum.amount??0));}async dailyLimit(tenantId:string,santriId:string){const santri=await this.tx().santri.findUnique({where:{id:santriId},select:{dailySpendLimit:true}});if(santri?.dailySpendLimit!==null&&santri?.dailySpendLimit!==undefined)return number(santri.dailySpendLimit);const tenant=await this.tx().tenant.findUnique({where:{id:tenantId},select:{defaultDailyLimit:true}});return number(tenant?.defaultDailyLimit??0);}async appendBalancedEntries(entries:[LedgerEntry,LedgerEntry]){await this.tx().ledgerEntry.createMany({data:entries.map(entry=>({tenantId:this.tenantId(),journalId:entry.journalId,accountId:entry.accountId,entryType:entry.entryType,amount:entry.amount,balanceSnapshot:entry.balanceSnapshot,description:entry.description}))});}async createTransaction(transaction:LedgerTransaction){await this.tx().transaction.create({data:{id:transaction.id,tenantId:transaction.tenantId,journalId:transaction.journalId,transactionType:transaction.type,cardId:null,santriId:transaction.santriId,merchantId:transaction.merchantId,terminalId:transaction.terminalId,operatorId:transaction.operatorId,withdrawalReason:transaction.withdrawalReason,amount:transaction.amount,status:transaction.status,idempotencyKey:transaction.idempotencyKey,createdAt:transaction.createdAt}});}async cashTopupLimit(tenantId:string){const tenant=await this.tx().tenant.findUnique({where:{id:tenantId},select:{cashTopupLimitPerTx:true}});return number(tenant?.cashTopupLimitPerTx??0);}async cashWithdrawalLimit(tenantId:string){const tenant=await this.tx().tenant.findUnique({where:{id:tenantId},select:{cashWithdrawalLimitPerTx:true}});return number(tenant?.cashWithdrawalLimitPerTx??0);}async appendAudit(input:{tenantId:string;action:'TRANSACTION_CHARGE'|'TRANSACTION_VOID';resourceId:string;metadata:Record<string,unknown>}){await this.tx().auditLog.create({data:{tenantId:input.tenantId,actorType:'SYSTEM',action:input.action,resourceType:'transaction',resourceId:input.resourceId,metadata:input.metadata}});}uuid(){return crypto.randomUUID();}private transaction(row:any):LedgerTransaction{return {id:row.id,journalId:row.journalId,idempotencyKey:row.idempotencyKey,tenantId:row.tenantId,santriId:row.santriId,amount:number(row.amount),type:row.transactionType,status:row.status,merchantId:row.merchantId,terminalId:row.terminalId??undefined,operatorId:row.operatorId??undefined,withdrawalReason:row.withdrawalReason??undefined,createdAt:row.createdAt};}}
