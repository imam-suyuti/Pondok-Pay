import type {FastifyInstance} from 'fastify';import {z} from 'zod';import {requireRoles} from '../../middlewares/auth-guard.js';
export interface SantriExitPort {deactivateSantriExit(santriId:string,staffId:string,reason:'GRADUATED'|'WITHDRAWN'|'OTHER'):Promise<{cardsDeactivated:unknown[]}>;}
export interface DailyLimitPort {setLimit(input:{tenantId:string;actor:{id:string;type:'STAFF'|'WALI_SANTRI'};santriId:string;amount:number}):Promise<void>;}
const params=z.object({id:z.string().uuid()}),body=z.object({amount:z.number().int().nonnegative()}),exitBody=z.object({reason:z.enum(['GRADUATED','WITHDRAWN','OTHER'])});
export async function registerSantriController(app:FastifyInstance,deps:{dailyLimit:DailyLimitPort;exit?:SantriExitPort}){app.patch('/v1/santri/:id/daily-limit',{preHandler:requireRoles('ADMIN_PESANTREN','WALI_SANTRI')},async req=>{const {id}=params.parse(req.params),{amount}=body.parse(req.body),auth=req.auth!;await deps.dailyLimit.setLimit({tenantId:auth.tenant_id!,actor:{id:auth.sub,type:auth.role==='WALI_SANTRI'?'WALI_SANTRI':'STAFF'},santriId:id,amount});return {success:true,data:{santri_id:id,daily_spend_limit:amount},meta:{requestId:req.id,timestamp:new Date().toISOString()}};});if(deps.exit)app.post('/v1/santri/:id/exit',{preHandler:requireRoles('ADMIN_PESANTREN')},async req=>{const {id}=params.parse(req.params),{reason}=exitBody.parse(req.body),result=await deps.exit!.deactivateSantriExit(id,req.auth!.sub,reason);return {success:true,data:{santri_id:id,cards_deactivated:result.cardsDeactivated.length},meta:{requestId:req.id,timestamp:new Date().toISOString()}};});}
