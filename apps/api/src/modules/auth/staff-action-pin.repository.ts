import argon2 from 'argon2';import {withTenant} from '../../db/client.js';import type {StaffActionPinRepository} from './staff-action-pin.service.js';
export class PrismaStaffActionPinRepository implements StaffActionPinRepository {async findActiveStaffActionUser(staffId:string,tenantId:string){const user=await withTenant<any>(tenantId,tx=>tx.staffUser.findFirst({where:{id:staffId,tenantId,status:'ACTIVE',role:{in:['ADMIN_PESANTREN','OPERATOR_MERCHANT']}},select:{id:true,role:true,merchantScope:true,actionPinHash:true}}));return user?{id:user.id,role:user.role,merchantScope:user.merchantScope,actionPinHash:user.actionPinHash}:undefined;}async verifyActionPin(encryptedPin:string,hash:string){return argon2.verify(hash,encryptedPin);}}
