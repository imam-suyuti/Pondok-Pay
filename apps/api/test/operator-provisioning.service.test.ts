import {describe,expect,it} from 'vitest';import {OperatorProvisioningError,OperatorProvisioningService,type OperatorProvisioningRepository} from '../src/modules/terminals/operator-provisioning.service.js';
class Repo implements OperatorProvisioningRepository {audit:unknown[]=[];async isAdminInTenant(){return true}async isMerchantInTenant(){return true}async hashActionPin(pin:string){return `argon2:${pin}`}async createOperator(i:any){return {id:'operator',fullName:i.fullName,merchantScope:i.merchantScope}}async appendAudit(i:unknown){this.audit.push(i)}}
describe('Merchant operator provisioning',()=>{it('creates a scoped terminal-only operator with hashed six digit action PIN',async()=>{const repo=new Repo();const operator=await new OperatorProvisioningService(repo).create({adminId:'admin',tenantId:'tenant',fullName:'Siti Aminah',phone:'0812',merchantScope:['merchant'],actionPin:'482917'});expect(operator).toMatchObject({id:'operator',merchantScope:['merchant']});expect(repo.audit).toHaveLength(1);});it('rejects missing scope and invalid action PIN',async()=>await expect(new OperatorProvisioningService(new Repo()).create({adminId:'admin',tenantId:'tenant',fullName:'Siti',phone:'0812',merchantScope:[],actionPin:'123'})).rejects.toBeInstanceOf(OperatorProvisioningError));});
