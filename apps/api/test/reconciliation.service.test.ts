import {describe,expect,it} from 'vitest';import {ReconciliationService,type ReconciliationRepository} from '../src/modules/reports/reconciliation.service.js';
class Repo implements ReconciliationRepository {debits=10000;credits=10000;async isAdminInTenant(){return true}async sumLedgerEntries(i:{entryType:'DEBIT'|'KREDIT'}){return i.entryType==='DEBIT'?this.debits:this.credits}async countTransactions(){return 3}}
describe('reconciliation report',()=>{it('reports a balanced double-entry ledger',async()=>expect(await new ReconciliationService(new Repo()).report({tenantId:'tenant',staffId:'staff',from:new Date('2026-07-01'),to:new Date('2026-07-01')})).toMatchObject({debits:10000,credits:10000,isBalanced:true,difference:0}));it('surfaces an imbalance without mutating the ledger',async()=>{const repo=new Repo();repo.credits=9000;expect((await new ReconciliationService(repo).report({tenantId:'tenant',staffId:'staff',from:new Date('2026-07-01'),to:new Date('2026-07-01')})).isBalanced).toBe(false);});});
