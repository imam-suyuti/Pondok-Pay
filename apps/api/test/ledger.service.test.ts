import { describe, expect, it } from 'vitest';
import { LedgerError, LedgerService, type LedgerAccount, type LedgerCard, type LedgerEntry, type LedgerRepository, type LedgerTransaction } from '../src/modules/ledger/ledger.service.js';
class InMemoryLedgerRepository implements LedgerRepository {
 private queue=Promise.resolve(); readonly entries:LedgerEntry[]=[]; readonly transactions:LedgerTransaction[]=[]; readonly audits:unknown[]=[]; private serial=0;
 card:LedgerCard={id:'card',tenantId:'tenant',santriId:'santri',status:'ACTIVE',pinHash:'pin'}; accounts:LedgerAccount[]=[{id:'santri-account',tenantId:'tenant',entityType:'SANTRI',entityId:'santri'},{id:'merchant-account',tenantId:'tenant',entityType:'MERCHANT',entityId:'merchant'},{id:'operating-account',tenantId:'tenant',entityType:'PESANTREN_OPERATING_CASH',entityId:'tenant'},{id:'pool-account',tenantId:'tenant',entityType:'PESANTREN_POOL',entityId:'tenant'}]; constructor(initialBalance=10000) {this.entries.push({journalId:'opening',accountId:'santri-account',entryType:'KREDIT',amount:initialBalance,balanceSnapshot:initialBalance,description:'opening'});}
 async runSerializable<T>(_tenantId:string,work:()=>Promise<T>){let release!:()=>void;const next=new Promise<void>(resolve=>release=resolve);const previous=this.queue;this.queue=next;await previous;try{return await work();}finally{release();}}
 async findTransactionByIdempotency(key:string){return this.transactions.find(t=>t.idempotencyKey===key)} async findTransactionById(id:string){return this.transactions.find(t=>t.id===id)} async markTransactionVoided(id:string){const tx=this.transactions.find(t=>t.id===id);if(tx)tx.status='VOIDED';} async lockCard(){return this.card;} async verifyPin(pin:string){return pin==='valid';} async lockAccount(_tenant:string,type:'SANTRI'|'MERCHANT',id:string){return this.accounts.find(a=>a.entityType===type&&a.entityId===id)} async runningBalance(accountId:string){return this.entries.filter(e=>e.accountId===accountId).reduce((balance,e)=>balance+(e.entryType==='KREDIT'?e.amount:-e.amount),0)} async successfulSalesToday(){return this.transactions.reduce((total,t)=>total+t.amount,0)} async dailyLimit(){return 1_000_000;} async cashTopupLimit(){return 500_000;} async cashWithdrawalLimit(){return 200_000;} async appendBalancedEntries(entries:[LedgerEntry,LedgerEntry]){this.entries.push(...entries)} async createTransaction(transaction:LedgerTransaction){this.transactions.push(transaction)} async appendAudit(input:unknown){this.audits.push(input)} uuid(){return `id-${++this.serial}`;}
}
describe('LedgerService processCharge',()=>{it('creates immutable balanced entries and returns its idempotent result',async()=>{const repo=new InMemoryLedgerRepository();const service=new LedgerService(repo);const command={tenantId:'tenant',cardId:'card',encryptedPin:'valid',merchantId:'merchant',amount:5000,idempotencyKey:'one'};const first=await service.processCharge(command);const replay=await service.processCharge(command);expect(first.newBalance).toBe(5000);expect(replay).toMatchObject({...first,idempotent:true});expect(repo.entries).toHaveLength(3);expect(repo.entries.slice(-2).map(e=>e.entryType)).toEqual(['DEBIT','KREDIT']);expect(repo.transactions).toHaveLength(1);});it('allows only one of 50 concurrent charges against an exact balance',async()=>{const repo=new InMemoryLedgerRepository(10_000);const service=new LedgerService(repo);const results=await Promise.allSettled(Array.from({length:50},(_,i)=>service.processCharge({tenantId:'tenant',cardId:'card',encryptedPin:'valid',merchantId:'merchant',amount:10_000,idempotencyKey:`concurrent-${i}`})));expect(results.filter(result=>result.status==='fulfilled')).toHaveLength(1);expect(results.filter(result=>result.status==='rejected'&&(result.reason as LedgerError).code==='INSUFFICIENT_BALANCE')).toHaveLength(49);expect(await repo.runningBalance('santri-account')).toBe(0);});it('pays out a merchant balance through an immutable settlement journal',async()=>{const repo=new InMemoryLedgerRepository(0);repo.entries.push({journalId:'merchant-opening',accountId:'merchant-account',entryType:'KREDIT',amount:7000,balanceSnapshot:7000,description:'opening'});const result=await new LedgerService(repo).processSettlementPayout({tenantId:'tenant',merchantId:'merchant',staffId:'staff',amount:7000});expect(result.journalId).toBeTruthy();expect(await repo.runningBalance('merchant-account')).toBe(0);expect(await repo.runningBalance('operating-account')).toBe(7000);});it('tops up through an Admin terminal by debiting PESANTREN_POOL, not a merchant',async()=>{const repo=new InMemoryLedgerRepository(0);const result=await new LedgerService(repo).processTerminalTopup({tenantId:'tenant',cardId:'card',amount:5000,merchantId:null,terminalType:'ADMIN',terminalId:'admin-terminal',operatorId:'operator',idempotencyKey:'admin-topup'});expect(result.newBalance).toBe(5000);expect(repo.entries.slice(-2)).toMatchObject([{accountId:'santri-account',entryType:'KREDIT'},{accountId:'pool-account',entryType:'DEBIT'}]);});it('requires PIN and prevents terminal withdrawal beyond its configured limit',async()=>{const service=new LedgerService(new InMemoryLedgerRepository(300000));await expect(service.processTerminalWithdrawal({tenantId:'tenant',cardId:'card',encryptedPin:'invalid',amount:1,reason:'Keperluan pulang',merchantId:'merchant',terminalId:'terminal',operatorId:'operator',idempotencyKey:'bad-pin'})).rejects.toMatchObject({code:'INVALID_PIN'});await expect(service.processTerminalWithdrawal({tenantId:'tenant',cardId:'card',encryptedPin:'valid',amount:200001,reason:'Keperluan pulang',merchantId:'merchant',terminalId:'terminal',operatorId:'operator',idempotencyKey:'over-limit'})).rejects.toMatchObject({code:'WITHDRAWAL_LIMIT_EXCEEDED'});});it('voids a sale with reversal entries without deleting the original transaction',async()=>{const repo=new InMemoryLedgerRepository();const service=new LedgerService(repo);const sale=await service.processCharge({tenantId:'tenant',cardId:'card',encryptedPin:'valid',merchantId:'merchant',amount:5000,idempotencyKey:'voidable',now:new Date('2026-07-01T00:00:00Z')});const reversal=await service.voidTransaction({tenantId:'tenant',transactionId:sale.transactionId,staffId:'staff',reason:'Salah input',now:new Date('2026-07-01T00:05:00Z'),voidWindowMinutes:15});expect(reversal.journalId).not.toBe(sale.journalId);expect(repo.transactions[0].status).toBe('VOIDED');expect(repo.entries).toHaveLength(5);expect(await repo.runningBalance('santri-account')).toBe(10000);});});
