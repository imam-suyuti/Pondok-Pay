import { describe, expect, it } from 'vitest'; import { readFileSync } from 'node:fs'; import { resolve } from 'node:path';
const sql=readFileSync(resolve(import.meta.dirname,'../prisma/migrations/20260722110000_ledger_core/migration.sql'),'utf8');
describe('ledger core migration safeguards',()=>{it('prevents UPDATE and DELETE on immutable ledger and audit tables',()=>{expect(sql).toContain('ledger_entries_immutable BEFORE UPDATE OR DELETE');expect(sql).toContain('audit_logs_immutable BEFORE UPDATE OR DELETE');});for(const table of ['cards','merchants','terminals','accounts','ledger_entries','transactions','merchant_settlement_invoices'])it(`forces tenant RLS for ${table}`,()=>{expect(sql).toContain(`ALTER TABLE ${table} FORCE ROW LEVEL SECURITY`);expect(sql).toContain(`tenant_isolation_${table}`);});});
