import Fastify from 'fastify';import {describe,expect,it} from 'vitest';import {registerCardRegistrationController} from '../src/modules/cards/card-registration.controller.js';
describe('card registration HTTP controller',()=>{it('only accepts a matching authenticated Admin terminal for card scan',async()=>{const app=Fastify({genReqId:()=> 'request-id'});await registerCardRegistrationController(app,{tenantGuard:{assertActive:async()=>{}},terminalAuth:{authenticate:async()=>({id:'terminal',tenantId:'tenant',terminalType:'ADMIN',deviceId:'device'})},registration:{handleCardScan:async()=>({id:'session'}),selectSantriForRegistration:async()=>{},finalizeCardRegistration:async()=>({id:'card'})}});const response=await app.inject({method:'POST',url:'/v1/terminal/card-scan',headers:{'x-device-id':'device','x-device-token':'token'},payload:{device_id:'device',card_uid:'04A3B2C1'}});expect(response.statusCode).toBe(200);expect(response.json()).toMatchObject({success:true,data:{session_id:'session'},meta:{requestId:'request-id'}});await app.close();});});
