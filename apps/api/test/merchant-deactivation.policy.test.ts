import {describe,expect,it} from 'vitest';import {calculateManualMerchantClosure} from '../src/modules/merchants/merchant-deactivation.policy.js';
describe('manual merchant deactivation policy',()=>{const base={feePerMerchant:25000,feeCutoffDay:12,writeoffThreshold:50000};it('records, rather than writes off, receivables above threshold',()=>expect(calculateManualMerchantClosure({...base,balance:-50001,dayOfMonth:20})).toEqual({feeCharged:0,settleAmount:0,unsettledReceivable:50001}));it('writes off negative balance at threshold or below',()=>expect(calculateManualMerchantClosure({...base,balance:-50000,dayOfMonth:20})).toEqual({feeCharged:0,settleAmount:0,unsettledReceivable:0}));it('settles positive balance before cutoff without fee',()=>expect(calculateManualMerchantClosure({...base,balance:60000,dayOfMonth:11})).toEqual({feeCharged:0,settleAmount:60000,unsettledReceivable:0}));it('charges fee and settles remainder after cutoff',()=>expect(calculateManualMerchantClosure({...base,balance:60000,dayOfMonth:12})).toEqual({feeCharged:25000,settleAmount:35000,unsettledReceivable:0}));});
