import { describe, expect, it } from 'vitest';
import { CardPinError, CardPinService, type CardPinRepository } from '../src/modules/cards/card-pin.service.js';
class Repo implements CardPinRepository {type:'MERCHANT'|'ADMIN'='ADMIN';updated='';audits:unknown[]=[];async runSerializable<T>(fn:()=>Promise<T>){return fn()}async terminalType(){return this.type}async lockActiveCardByUid(){return {id:'card',tenantId:'tenant',santriId:'santri',cardUid:'uid',status:'ACTIVE' as const}}async hashPin(value:string){return `argon2:${value}`}async updatePinHash(_tenant:string,_id:string,hash:string){this.updated=hash}async santriName(_tenant:string){return 'Ahmad Fauzi'}async appendAudit(input:unknown){this.audits.push(input)}}
describe('physical card PIN reset',()=>{it('only permits Admin terminal and writes an audit event',async()=>{const repo=new Repo();const result=await new CardPinService(repo).resetCardPin({tenantId:'tenant',cardUid:'uid',newPinHashLocal:'sha256-terminal',terminalId:'terminal',staffId:'staff'});expect(result).toEqual({status:'SUCCESS',santriName:'Ahmad Fauzi'});expect(repo.updated).toBe('argon2:sha256-terminal');expect(repo.audits).toHaveLength(1);});it('rejects merchant terminal',async()=>{const repo=new Repo();repo.type='MERCHANT';await expect(new CardPinService(repo).resetCardPin({tenantId:'tenant',cardUid:'uid',newPinHashLocal:'hash',terminalId:'terminal',staffId:'staff'})).rejects.toBeInstanceOf(CardPinError);});});
