import {describe,expect,it} from 'vitest';import {WaliTenantSessionError,WaliTenantSessionService,type WaliTenantSessionRepository} from '../src/modules/wali-santri/wali-tenant-session.service.js';
class Repo implements WaliTenantSessionRepository {async listRelatedTenants(){return [{id:'active',name:'Pondok Al-Falah',status:'ACTIVE' as const},{id:'suspended',name:'Pondok Internal',status:'SUSPENDED' as const}]}relation=true;statusValue:'ACTIVE'|'SUSPENDED'='ACTIVE';async hasActiveRelation(){return this.relation}async tenantStatus(){return this.statusValue}}
describe('Wali tenant session selection',()=>{it('lists only related active tenants without exposing suspended tenant details',async()=>expect(await new WaliTenantSessionService(new Repo()).listAvailableTenants('wali')).toEqual([{id:'active',name:'Pondok Al-Falah'}]));it('creates only an active related tenant context',async()=>expect(await new WaliTenantSessionService(new Repo()).selectTenant('wali','tenant')).toEqual({tenantId:'tenant'}));it('rejects suspended tenant selection',async()=>{const repo=new Repo();repo.statusValue='SUSPENDED';await expect(new WaliTenantSessionService(repo).selectTenant('wali','tenant')).rejects.toBeInstanceOf(WaliTenantSessionError);});});
