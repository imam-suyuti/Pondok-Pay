import {describe,expect,it} from 'vitest';import {TerminalProvisioningError,TerminalProvisioningService,type TerminalProvisioningRepository} from '../src/modules/terminals/terminal-provisioning.service.js';
class Repo implements TerminalProvisioningRepository {audit=0;async isAdminInTenant(){return true}async isMerchantInTenant(){return true}async hashDeviceToken(t:string){return `hash:${t}`}async createTerminal(i:any){return {id:'terminal',deviceId:i.deviceId,terminalType:i.terminalType,merchantId:i.merchantId}}async appendAudit(){this.audit++}}
describe('terminal provisioning',()=>{it('issues a secret token once and stores only its hash',async()=>{const repo=new Repo(),result=await new TerminalProvisioningService(repo).provision({tenantId:'tenant',staffId:'staff',terminalType:'ADMIN',merchantId:null,deviceId:'ADMIN-01'});expect(result.terminal).toMatchObject({terminalType:'ADMIN'});expect(result.deviceToken.length).toBeGreaterThan(20);expect(repo.audit).toBe(1);});it('rejects Merchant terminal without a merchant scope',async()=>await expect(new TerminalProvisioningService(new Repo()).provision({tenantId:'tenant',staffId:'staff',terminalType:'MERCHANT',merchantId:null,deviceId:'TERM'})).rejects.toBeInstanceOf(TerminalProvisioningError));});
