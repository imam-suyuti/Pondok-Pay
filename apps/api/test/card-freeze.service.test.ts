import {describe,expect,it} from 'vitest';import {CardFreezeService,type CardFreezeRepository} from '../src/modules/cards/card-freeze.service.js';
class Repo implements CardFreezeRepository {frozen=false;audit:unknown[]=[];async findCard(){return {id:'card',tenantId:'tenant',santriId:'santri',status:'ACTIVE' as const}}async isWaliResponsibleFor(){return true}async isAdminInTenant(){return true}async freeze(){this.frozen=true}async appendAudit(a:unknown){this.audit.push(a)}}
describe('card loss freeze policy',()=>{it('records wali report without granting direct freeze authority',async()=>{const repo=new Repo();expect(await new CardFreezeService(repo).requestFreeze({tenantId:'tenant',actor:{id:'wali',type:'WALI_SANTRI'},cardId:'card'})).toEqual({status:'REPORTED'});expect(repo.frozen).toBe(false);expect(repo.audit).toHaveLength(1);});it('lets tenant admin freeze and audits the critical mutation',async()=>{const repo=new Repo();expect(await new CardFreezeService(repo).requestFreeze({tenantId:'tenant',actor:{id:'staff',type:'STAFF'},cardId:'card'})).toEqual({status:'FROZEN'});expect(repo.frozen).toBe(true);});});
