import {describe,expect,it} from 'vitest';import {OperatorPinManagementError,OperatorPinManagementService,type OperatorPinManagementRepository} from '../src/modules/terminals/operator-pin-management.service.js';
class Repo implements OperatorPinManagementRepository {hash='';audit=0;async isAdminInTenant(){return true}async lockOperator(){return {id:'operator',tenantId:'tenant',role:'OPERATOR_MERCHANT' as const,status:'ACTIVE' as const}}async hashActionPin(pin:string){return `argon2:${pin}`}async updateActionPin(_tenant:string,_id:string,h:string){this.hash=h}async appendAudit(){this.audit++}}
describe('operator action PIN management',()=>{it('allows tenant Admin to reset individual six digit Operator PIN with audit trail',async()=>{const repo=new Repo();await new OperatorPinManagementService(repo).reset({tenantId:'tenant',adminId:'admin',operatorId:'operator',newPin:'482917'});expect(repo.hash).toBe('argon2:482917');expect(repo.audit).toBe(1);});it('rejects PIN outside six digits',async()=>await expect(new OperatorPinManagementService(new Repo()).reset({tenantId:'tenant',adminId:'admin',operatorId:'operator',newPin:'123'})).rejects.toBeInstanceOf(OperatorPinManagementError));});
